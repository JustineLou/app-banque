{% extends 'base.html.twig' %}

{% block title %}Editer une transaction{% endblock %}

{% block body %}
    <h1>Editer une transaction</h1>

    {{ include('transaction/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('transaction_index') }}">back to list</a>

    {{ include('transaction/_delete_form.html.twig') }}
{% endblock %}
